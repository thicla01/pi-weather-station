SourceBadge — the leading pill on every alert banner and radar line, naming where the data comes from. Mono 11 px uppercase, --c-accent-soft fill.

<SourceBadge source="ECCC" />
<SourceBadge source="RADAR" />
<SourceBadge source="AQHI" />
<SourceBadge source="TEST" variant="test" />

Rules: honest about origin — a new source gets a 3–5 letter uppercase tag (never LOCAL / AUTO). TEST is a qualifier, never a source: outlined, dimmed, placed NEXT TO the source badge with a "TEST ·" title prefix. All banner badges share this one visual — reuse, don't fork.
