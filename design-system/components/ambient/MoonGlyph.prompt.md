MoonGlyph — the only moon icon in the system; used in AstroMetaLine and the moon popover.

<MoonGlyph fraction={0.18} />                        {/* waxing crescent */}
<MoonGlyph fraction={0.62} size="20px" />            {/* waning gibbous */}
<MoonGlyph fraction={0.5} title="Full moon · 100%" />

Rules: never an emoji. Dark disc + bright lit side in all four palettes (nightRed: red on deep red). Lit on the right while waxing. Size in em so it rides the line's font-size.
