ConfidencePill — how sure the radar heuristic is; sits beside a SourceBadge.

<ConfidencePill confidence={82} />
<ConfidencePill confidence={55} />
<ConfidencePill confidence={30} />

Rules: high confidence is NOT green — the warm-grey palette has no success token; calm accent-soft says "fine". The NowcastLine uses the same buckets as a 7 px dot (accent / warn / danger).
